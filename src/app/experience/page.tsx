import Experience from '@/pages/Experience';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Experience - Erik Filipov',
	description: '',
};

export default function ExperiencePage() {
	return <Experience />;
}