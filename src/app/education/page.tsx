import Education from '@/pages/Education';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Education - Erik Filipov',
	description: '',
};

export default function EducationPage() {
	return <Education />;
}